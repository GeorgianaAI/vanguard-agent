# Dependency risk register

**As of 2026-04-03:** No deferred high/critical advisories tracked outside CI; root and `mcp-server/` both run `npm audit --audit-level=high` in CI. See [dependency-audit-policy.md](./dependency-audit-policy.md). If a finding must be accepted, record it in GitHub Issues with mitigation and review date rather than duplicating status here.
